import 'package:flutter/material.dart';

class MusicQueue extends StatelessWidget {
  const MusicQueue({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
